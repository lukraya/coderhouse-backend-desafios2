<header>
    <h1>Bienvenide, {{username}}</h1>
    {{!-- <p>Correo electrónico: {{email}}</p>
    <p>Foto: </p>
    <img src="{{picture}}" alt="foto de perfil" width="50px" height="50px"> --}}
    <form action="/logout" method="get">
        <button type="submit">Desloguear</button>
    </form>
</header>